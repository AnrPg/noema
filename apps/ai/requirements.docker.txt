# =============================================================================
# MANTHANEIN AI SERVICE - LIGHTWEIGHT REQUIREMENTS FOR DOCKER
# =============================================================================
# This is a minimal set of dependencies for running the AI service in Docker
# Full dependencies are in pyproject.toml

# Core framework
fastapi>=0.109.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0
pydantic-settings>=2.1.0
python-multipart>=0.0.6
httpx>=0.26.0
aiofiles>=23.2.1

# AI/ML - API-based (lightweight)
openai>=1.10.0
anthropic>=0.18.0
google-generativeai>=0.3.0

# Vector DB
qdrant-client>=1.7.0

# Document parsing
pypdf>=3.17.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.23
markdown>=3.5.0
beautifulsoup4>=4.12.0
lxml>=5.1.0

# OCR
pytesseract>=0.3.10
pdf2image>=1.17.0
pillow>=10.2.0

# Audio (lightweight - no whisper, use API)
pydub>=0.25.1

# Utilities
python-jose[cryptography]>=3.3.0
redis>=5.0.0
boto3>=1.34.0
tenacity>=8.2.0
structlog>=24.1.0

# Note: Heavy ML dependencies (torch, sentence-transformers, spacy, whisper)
# are excluded for Docker size. Use cloud APIs or add them for GPU instances.
