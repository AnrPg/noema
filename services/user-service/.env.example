# User Service Environment Variables# ==============================================================================



































LOG_PRETTY=trueLOG_LEVEL=debug# LoggingLOCKOUT_DURATION_MINUTES=15MAX_LOGIN_ATTEMPTS=5BCRYPT_ROUNDS=12# SecurityJWT_AUDIENCE=noema.appJWT_ISSUER=noema.appREFRESH_TOKEN_EXPIRES_IN=7dACCESS_TOKEN_EXPIRES_IN=15mREFRESH_TOKEN_SECRET=your-refresh-token-secret-min-32-charsACCESS_TOKEN_SECRET=your-access-token-secret-min-32-chars# JWT Secrets (generate with: openssl rand -base64 64)REDIS_STREAM_MAX_LEN=10000REDIS_EVENT_STREAM=noema:events:user-serviceREDIS_URL=redis://localhost:6379# RedisDATABASE_URL=postgresql://noema:noema@localhost:5432/noema_users?schema=public# Database (PostgreSQL)PORT=3001HOST=0.0.0.0# ServerSERVICE_VERSION=1.0.0NODE_ENV=development# Service# Copy this file to .env and fill in the values# Noema User Service - Environment Variables
# ==============================================================================
# Copy this file to .env and fill in your values

# ------------------------------------------------------------------------------
# Service Configuration
# ------------------------------------------------------------------------------
SERVICE_NAME=user-service
SERVICE_VERSION=1.0.0
NODE_ENV=development

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
HOST=0.0.0.0
PORT=3001

# ------------------------------------------------------------------------------
# Database Configuration
# ------------------------------------------------------------------------------
# PostgreSQL connection string
DATABASE_URL=postgresql://noema:noema@localhost:5432/noema_users?schema=public

# ------------------------------------------------------------------------------
# Redis Configuration
# ------------------------------------------------------------------------------
REDIS_URL=redis://localhost:6379
REDIS_EVENT_STREAM=noema:events:user-service
REDIS_STREAM_MAX_LEN=10000

# ------------------------------------------------------------------------------
# JWT Configuration
# ------------------------------------------------------------------------------
# IMPORTANT: Generate strong secrets for production!
# Use: openssl rand -base64 64
ACCESS_TOKEN_SECRET=your-super-secret-access-token-key-min-32-chars
REFRESH_TOKEN_SECRET=your-super-secret-refresh-token-key-min-32-chars
ACCESS_TOKEN_EXPIRES_IN=15m
REFRESH_TOKEN_EXPIRES_IN=7d
JWT_ISSUER=noema.app
JWT_AUDIENCE=noema.app

# ------------------------------------------------------------------------------
# Security Configuration
# ------------------------------------------------------------------------------
BCRYPT_ROUNDS=12
MAX_LOGIN_ATTEMPTS=5
LOCKOUT_DURATION_MINUTES=15

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
LOG_LEVEL=debug
LOG_PRETTY=true
